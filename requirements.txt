# khipu-sda-core / szl_mosaic — clean-room anomaly/SDA core engine
# Sovereign, own-metal, runnable in-sandbox. Pinned-by-major; permissive only.

numpy>=2.5.1
scipy>=1.18.0            # Hungarian assignment (track fusion); BSD-3
scikit-learn>=1.9.0      # IsolationForest, StandardScaler; BSD-3
matplotlib>=3.11.1        # validation figure; PSF/BSD-compatible
sgp4>=2.27             # python-sgp4 (brandon-rhodes); MIT — orbit propagation
torch>=2.13.0             # OPTIONAL tiny autoencoder; BSD-3-style. Falls back to a
                       # pure-numpy PCA autoencoder if torch is absent.

# Shared receipt/attestation contract — the SINGLE SOURCE OF TRUTH for the
# in-toto Statement v1 shape this package emits (szl_sda_envelope.make_intoto_receipt
# delegates to szl_receipt.attest.build_statement). Public, Apache-2.0, pinned by
# tag. The import is lazy so absence degrades to an identical local fallback
# (honest, non-breaking).
szl-receipt @ git+https://github.com/szl-holdings/szl-receipt.git@v0.2.0

# --- DEV/TEST ---
pytest>=9.1.1

# ============================================================================
# CLEAN-ROOM LINEAGE (methods adopted, NOT vendored as runtime deps here):
#   PyOD (BSD-2), PyGOD (BSD-2), Merlion (BSD-3), TODS (Apache-2.0),
#   tsod (MIT), GDN (MIT), GraGOD (MIT).
# These libraries' METHODS/IDEAS inspired the clean-room detectors in this
# package; their packages can be added as direct deps when wiring the full
# detector bank. See ATTRIBUTION.md + THIRD_PARTY_NOTICES.
#
# DELIBERATELY EXCLUDED: alibi-detect — relicensed to BSL-1.1 (2024-01-22).
# DO NOT add alibi-detect. CI must block it.
# ============================================================================
